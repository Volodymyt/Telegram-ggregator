"""Canonical login bootstrap placeholder."""


def run_login() -> None:
    """Route login startup through the canonical bootstrap boundary."""

    raise SystemExit(
        "Login bootstrap is not implemented yet. Use the canonical "
        "telegram_aggregator.bootstrap.run_login entrypoint for future session wiring."
    )
